// import Notification from "@components/UI/notification/page";

// interface useNotificationProps {
//   mode: "info" | "success" | "error";
//   message: string;
// }

// export default function useNotification() {
//   function notify({ mode = "info", message }: useNotificationProps) {
//     return Notification({ mode, message });
//   }
//   return { notify };
// }
